# ============================================================
# VARIABILE DE MEDIU PENTRU RENDER — Payload CMS
# Copiază exact aceste valori în Render Dashboard → Environment
# ============================================================

# === Payload Secret (OBLIGATORIU — generează pe: openssl rand -base64 48) ===
PAYLOAD_SECRET=your-random-64-char-secret-here

# === Server URL (URL-ul serviciului tău Render) ===
NEXT_PUBLIC_SERVER_URL=https://your-app-name.onrender.com

# === Admin Account ===
ADMIN_EMAIL=admin@asigurari.ro
ADMIN_PASSWORD=ChangeThisPassword123!

# === MongoDB (direct — fără PostgreSQL) ===
MONGODB_URI=mongodb://192.168.0.31:27017/wib_test

# === AI Features ===
OPENAI_API_KEY=sk-...

# === CORS ===
CORS_ORIGIN=https://www.asigurari.ro

# === Email ===
EMAIL_FROM=noreply@asigurari.ro
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_SECURE=false

# === n8n ===
N8N_WEBHOOK_URL=http://n8n:5678

# === Preview ===
PREVIEW_SECRET=ee4016c8434b8bfac95a92cb7cc44bb9
SITE_URL=https://www.asigurari.ro

# ============================================================
# INSTRUCȚIUNI RENDER.COM:
# ============================================================
# 1. Build Command:  npm install && npm run build
# 2. Start Command:  node server.js
# 3. Node Version:   22
# 4. Port:           3000 (setează PORT=3000 sau lasă default)
# 5. Elimină variabilele PostgreSQL (DB_CLIENT, DB_HOST, etc.)
# 6. Generează PAYLOAD_SECRET: openssl rand -base64 48
# ============================================================
